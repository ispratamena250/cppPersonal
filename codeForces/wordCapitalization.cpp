#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_stdio(false);	
	cin.tie(NULL);
	cout.tie(NULL);

	string s;
	for(size_t i=0; i<s.size(); i++){
		if(i==0){
			cout << s[i] + '32';
		}
			cout << s[i];
	}
	cout << endl;

	return 0;
}
