#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	
	string s, aux="codeforces";
	int t;
	cin >> t;

	for(int i=0; i<t; i++){
		cin >> s;
		for(size_t j=0; j<s.size(); j++){
			if(s[j] == 
		}
	}

	return 0;
}
